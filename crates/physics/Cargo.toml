[package]
name = "physics"
version = "0.1.0"
edition = "2021"

[dependencies]
compute = { path = "../compute" }
bytemuck = { version = "1.15", features = ["derive"] }
glam = "0.27"

[dev-dependencies]
anyhow = "1.0"
criterion = "0.5"
ml = { path = "../ml" }
approx = "0.5"
